import { PageIntro } from "@/components/crm/page-intro";

export default function ContactsPage() {
  return (
    <PageIntro
      eyebrow="Contacts"
      title="Track real people, not just accounts"
      description="Contacts will hold decision-maker details, preferred channel, relationship notes, public profile links, and the interaction history needed for respectful, specific outreach."
    />
  );
}
