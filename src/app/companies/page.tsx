import { PageIntro } from "@/components/crm/page-intro";

export default function CompaniesPage() {
  return (
    <PageIntro
      eyebrow="Companies"
      title="Store durable context about each business"
      description="Companies become the anchor record for industry fit, location, notes, lead quality, proposal history, documents, and later active project and payment relationships."
    />
  );
}
